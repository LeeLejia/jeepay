# ui项目的根路径（相对于 docker-compose.yml 所在目录）
# 将 jeepay-ui 克隆到项目目录下后使用 . 即可，例如: git clone https://gitee.com/jeequan/jeepay-ui.git
# 若暂不需要前端，可注释掉 docker-compose.yml 中的 ui-payment、ui-manager、ui-merchant 服务
UI_BASE_DIR=.